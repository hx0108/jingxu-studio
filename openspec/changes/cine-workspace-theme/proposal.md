# Proposal

将评审原型 `designs/jingxu-studio-redesign/`（去 AI 味双方向，选定 CINE 剪辑台）落地到正式 Renderer：中性碳灰底 + 单一信号琥珀替换靛蓝/海军蓝体系，本地打包 IBM Plex + Noto Sans SC 字体，并修复 `prototype-alignment.css` 中 `--jx-*` 令牌从未定义导致边框/指示点静默丢失的缺陷。不改变信息架构、布局尺寸与任何行为，无 specs 增量。
