## 1. 依赖、Port 契约与架构基线

- [x] 1.1 核验百炼 OpenAI 兼容接口在 Node 22.16.0、TypeScript 下的传输与超时依赖，精确锁定版本到唯一 `pnpm-lock.yaml` 并记录核验日期；不得使用 `latest` 或第二锁文件。（Design §7、§9；AGENTS.md §11.3）✓ 核验日期 2026-08-12：Node v22.16.0 全局 fetch/AbortSignal.timeout 覆盖传输与超时，全仓零 SDK 依赖，本 Change 不引入新依赖、`pnpm-lock.yaml` 无变更（详见 design.md Risks「百炼依赖升级」核验条）。
- [x] 1.2 先添加架构边界测试：`packages/application`、`packages/domain` 不得导入百炼 SDK、`node:sqlite`、`model-adapters` 或 Electron 模块；`packages/model-adapters` 不得被 Application/Domain 反向依赖。（R: TextModelAdapter 契约必须屏蔽 Provider 专有结构 / 全部 Scenario；AGENTS.md §7.1、TECH_DESIGN v1.1 §4.3.3）✓ 已在 `eslint.config.mjs` 添加 application/domain zone（禁 electron/better-sqlite3/node:sqlite/openai/dashscope/@jingxu/persistence/@jingxu/model-adapters）与 model-adapters zone（禁 electron/better-sqlite3/node:sqlite/@jingxu/persistence）；正向探针验证两 zone 均触发 `no-restricted-imports`，现有代码零误报。
- [x] 1.3 在 Application 定义带 TSDoc 的 `TextModelPort`（`validateCredential`/`generate(signal)`/`normalizeError`）、`TextGenerationRequest`/`TextGenerationResult`/`NormalizedModelError` 类型与 `CredentialPort`；接口不得泄漏百炼类型、Row、Statement、连接、明文 Key 或可变 Buffer。（Design §1；TECH_DESIGN v1.1 §6.1）✓ 已实现：`packages/contracts/src/stage.ts`（ScriptStage zod enum，镜像 `prompt_templates.stage`）、`packages/application/src/ports/text-model/`（Port + 6 类型 + barrel + 类型测试）、`packages/application/src/ports/credential/`（Port + CredentialRef + barrel + 类型测试），均经 `tsc -b` + `vitest`(6/6) + `eslint` 验证通过。
- [x] 1.4 创建 `packages/model-adapters/`（Qwen + Mock）与 `packages/application/src/jobs/` 目录、strict tsconfig 与 workspace references；扩展 package 边界测试证明依赖方向。（AGENTS.md §7.1；TECH_DESIGN v1.1 §4.2）✓ 已建立 `@jingxu/model-adapters` strict 包、Application jobs 公开入口与根 solution reference；workspace frozen install、root `tsc -b`、ESLint 和双向非法导入探针均通过，未预建业务实现。

## 2. Job/Invocation Repository 与 UnitOfWork（复用 0001 表）

- [x] 2.1 先添加 `script_stage_jobs` Row mapper/Repository 集成测试：空表、领取原子性（`WHERE status='QUEUED'` 条件更新只命中一个领取方）、`UNIQUE(project_id,idempotency_key)` 幂等冲突、坏 `status`/`transport_attempts`/`structure_repair_attempts` 值归一化、参数绑定 SQL、无 `SELECT *`，且 Repository 外不见 Row/连接。（R: Job 领取必须原子且仅领取成功方调用 Provider；Design §2）
- [x] 2.2 先添加 `model_invocations` Repository 集成测试：写 `request_sent_at`、写响应 blob/hash/`response_complete_at`、写 `late_response_at`、`attempt_kind`/`transport_attempt` 约束、超时 `timeout_at` 与崩溃恢复查询矩阵的精确读取。（R: Provider 调用必须事务外，证据与版本短事务原子提交；R: 崩溃恢复必须按持久化证据确定终态；Design §2、§4）
- [x] 2.3 实现 Job/Invocation Repository 与短事务 `UnitOfWork`（复用既有单写连接、`BEGIN IMMEDIATE`），由 Application/JobRunner 拥有事务边界；Repository 不嵌套提交，persistence 边界归一化数据库异常，不修改 `0001_initial.sql`/`0002_project_command_receipts.sql` 或新增 migration。（Design §2；AGENTS.md §11.2、§12.2）
- [x] 2.4 添加应用级不变量测试：`transport_attempts` 仅 0–3、`structure_repair_attempts` 仅 0–1 与状态机一致；DB CHECK 与应用守卫共同阻断越界转移。（R: 重试与结构修复必须受状态机、次数与错误类型约束；AGENTS.md §11.1）

## 3. MockTextModelAdapter 与两层契约

- [x] 3.1 先添加 Mock 单元测试：按声明序列确定性输出 401、429、5xx、120 秒超时、非法 JSON、结构修复失败、STALE_INPUT、取消与取消后迟到响应；时间/ID/随机可注入；不访问真实网络（网络守卫为零调用）。（R: Job 基线必须以 Mock 全矩阵作为 AC-V1-04 证据；AGENTS.md §15.3）
- [x] 3.2 实现 `MockTextModelAdapter` 与可注入的时钟/响应序列；提供合法候选输出以驱动 `QUEUED→RUNNING→VALIDATING→SUCCEEDED` 成功路径。（Design §7）
- [x] 3.3 先添加两层契约单元测试：候选 Schema → 注入系统字段 → 正式 Schema → 集合校验的固定顺序；系统字段每次由 JobRunner 重新生成；候选非法但可修复触发最多一次 structure repair；第二次结构失败终态 `FAILED`；任何一层失败不创建版本。（R: 重试与结构修复必须受状态机、次数与错误类型约束；TECH_DESIGN v1.1 §6.1.1）

## 4. JobRunner 确定性编排

- [x] 4.1 先用 Fake Ports 添加 JobRunner 单元测试：领取条件更新只让一个 runner 胜出；Provider 调用在事务外；标记发送先于网络；响应证据与 Job→`VALIDATING` 一致；最终提交在单一 `UnitOfWork` 短事务原子完成，任一步失败整体回滚不留半个版本。（R: Job 领取必须原子；R: Provider 调用必须事务外；Design §3、§4）
- [x] 4.2 实现 `JobRunner` 主循环：带条件领取、经 `TextModelPort.generate(signal)` 事务外调用、`ModelInvocation` 证据落库、两层契约与最小 `JobCommitHandler` 提交；JobRunner 不直接执行 SQL 或导入 `node:sqlite`/Adapter。（Design §1、§3、§4；AGENTS.md §11.2）
- [x] 4.3 先添加重试策略单元测试：网络错误/429/5xx 触发最多 2 次 `TRANSPORT_RETRY` 且每次独立 Invocation；401/内容拒绝/上下文超限/结构校验失败直接 `FAILED` 不重试；structure repair ≤1 只修候选；终态不回退。（R: 重试与结构修复必须受状态机、次数与错误类型约束）
- [x] 4.4 先添加取消与迟到响应单元测试：取消先原子写 `cancel_requested_at`+`CANCELLED` 再 abort；迟到响应只写 hash/`late_response_at`，不写 parsed JSON/版本/重试；页面刷新不取消非取消 Job。（R: 取消与迟到响应不得创建版本或触发重试）
- [x] 4.5 先添加并发门集成测试：全局同时最多 1 个真实 `RUNNING` Job；同项目严格串行；Mock Job 复用同一并发门并可验证。（R: 真实 LLM Job 全局唯一且同项目严格串行；Design §5）

## 5. 崩溃恢复与 AC-V1-04 Mock 矩阵

- [x] 5.1 先按 TECH_DESIGN v1.1 §5.3.2 矩阵添加崩溃恢复集成测试逐条覆盖：`QUEUED` 无 Invocation 重新领取；`request_sent_at` 非空/`response_complete_at` 为空 → `FAILED`/`INTERRUPTED_UNKNOWN_OUTCOME` 禁止重发；完整响应 hash 正确 → 幂等重校验不重复版本；`cancel_requested_at` 非空保持 `CANCELLED`；超过 `deadline_at`/`timeout_at` → `FAILED`/`JOB_DEADLINE_EXCEEDED`；重启不重置 deadline/timeout。（R: 崩溃恢复必须按持久化证据确定终态且不自动重发未知请求）
- [x] 5.2 添加 AC-V1-04 Provider Contract/Integration：固定 Mock 依次模拟 401、429、5xx、120 秒超时、非法 JSON、结构修复失败、STALE_INPUT、取消、迟到响应与各崩溃点；断言终态不回退、不重复版本、原始输入不丢失、保留 Provider 任务 ID。（R: Job 基线必须以 Mock 全矩阵作为 AC-V1-04 证据；PRD v1.4 AC-V1-04、§9.4.2/§9.4.3）✓ 新增 `packages/model-adapters/src/mock/ac-v1-04-provider-matrix.integration.test.ts`：把**真实 `MockTextModelAdapter`** 接到**真实 `createJobRunner`**（内存版 Job/Invocation Repository + 快照回滚 UoW），用声明式 step 序列跑通 8 例全矩阵——401 `MODEL_CREDENTIAL_INVALID`（不重试直 FAILED）/429 `MODEL_RATE_LIMITED`（两次重试内 SUCCEEDED）/5xx `MODEL_PROVIDER_ERROR`（三次耗尽 FAILED + 终态不回退）/120s `MODEL_TIMEOUT`（注入 wait 不真实等待）/非法 JSON→结构修复 SUCCEEDED（修复 Invocation 新任务 ID 入版本）/结构修复失败 `STRUCTURE_REPAIR_FAILED`/STALE_INPUT（commit seam 拒绝→UoW 回滚、零版本、VALIDATING）/取消+迟到响应（先持久化 CANCELLED 再 abort、迟到只写 hash/late time、零版本）。每例断言四条横切不变量：终态不回退（再 run→NOT_CLAIMED）、不重复版本（versions 计数）、原始输入不丢失（每次 Invocation 留 `requestSnapshotJson`+`requestSha256`、Job `inputVersionsJson` 不变）、保留 Provider 任务 ID（成功/修复 Invocation 的 `providerRequestId` 与提交版本 `source_invocation_id` 对齐）。STALE_INPUT 真实哈希复检与终态化属 #6 业务 commit handler，#5 只证 JobRunner 回滚保证。验证：integration config 8/8；model-adapters `tsc` 0；ESLint 0。网络守卫（零 fetch）由 3.1 锁定同一适配器继承。

## 6. QwenTextModelAdapter、ProviderService 与凭据

- [x] 6.1 先添加 Qwen Adapter 单元测试：固定模型 `qwen3.7-plus-2026-05-26`、拒绝漂移别名、`response_format={"type":"json_object"}`、64K 输入超限阻断不裁剪、120s/300s 超时映射、401/429/5xx/超时/非法 JSON 归一化为稳定 `NormalizedModelError` 且不含 Authorization header/原始错误体。（R: Qwen Adapter 必须锁定固定模型与 JSON Mode 且归一化错误；Design §9）
- [x] 6.2 实现 `QwenTextModelAdapter`（百炼 OpenAI 兼容 Chat Completions，Base URL 由受校验配置派生），只支撑单次请求 + 归一化错误；transport retry 由 JobRunner 驱动。（Design §7、§9；AGENTS.md §11.3）
- [x] 6.3 先添加凭据单元/集成测试：`safeStorage` 不可用阻断保存不降级明文；SQLite 只存 `credential_ref`；UI 只显末 4 位；删除同步删密文并写审计；日志/诊断包/导出/Renderer 状态零完整 Key（白名单审计）。（R: 凭据必须经 safeStorage 加密且不回流 Renderer；AGENTS.md §13.2、§13.3）✓ 在 6.4（safeStorage 不可用阻断/密文独立/末四位/删除+审计）与 7.2（IPC view→DTO 仅 last4、Key+Auth+SQL 零泄漏）之上补齐白名单审计三件：(1) `credential-safety.integration.test.ts`——明文 Key 经 ref+last4 落库后，**原始 SQLite 文件字节不含明文 Key**（仅含不透明 ref 与末四位），`findById` 读回的 `ProviderProfile` 序列化无明文；(2) `credential-adapter.test.ts` +1 例——save/load/delete 全程 `console.*` 与 `stderr/stdout` 零明文 Key（当前零日志，作为未来回归守卫）；(3) `managed-paths.test.ts` +1 例——`root/secrets`（CredentialAdapter 密文目录）与 `diagnosticDirectory` 为并列 sibling，诊断打包范围无法触及密文（#5 无诊断/导出打包器，此为引入时的结构保证）。Renderer 状态零 Key 由 7.1/7.2 双端 strict Zod DTO + 冻结 preload 白名单覆盖。验证：root `tsc -b` 0；credential-adapter 4/4、credential-safety 2/2、managed-paths 8/8；ESLint 0。
- [x] 6.4 实现 Main `CredentialAdapter`（Electron `safeStorage`，密文独立保存）与 `ProviderService`（配置、凭据引用、数据处理提示、低成本 `testCredential` 连通性检查）；真实模型调用在本 Change 内仅限凭据验证。（Design §6、§7；PRD v1.4 §9.4.4）✓ CredentialAdapter、ProviderService（含 `saveProfile`/数据处理提示 `ProviderProfileConfig`）、provider_profiles 生产 Repository/UoW/Audit/Row-mapper 均完成；`ProviderProfile` 类型按 Option A 重构（`baseUrl`+`config`+`versionId`，`lastValidatedAt` 收敛进 config 单一来源），`credential_ref NOT NULL` 决定「行⟺凭据」语义（`saveCredential` 落行 / `deleteCredential` 删整行 + 审计）。验证：root `tsc -b` 0；ProviderService 单测 5/5；provider_profiles 集成 7/7（往返/upsert/删行+审计/UoW 回滚/空 ref 守卫/坏 Row/显式列+参数绑定）；ESLint 0。Main 组装在 7.4。

## 7. job/provider/events IPC、Preload 与启动门衔接

- [x] 7.1 先扩展 IPC/Preload Contract 测试：`job create/get/list/cancel/retry`、`provider getProfile/saveProfile/saveCredential/testCredential/deleteCredential`、`events.subscribeJobUpdates` 逐方法白名单、双端 strict Zod DTO、`AppError` 输出；Renderer 无通用 `send/on/invoke` 入口。（R: Job/Provider IPC 必须服从启动写门与幂等；TECH_DESIGN v1.1 §11）
- [x] 7.2 实现 IPC Host、Preload `window.jingxu` 冻结白名单与 `JobService`：Command 携带 `requestId`、修改命令携带 `expectedVersionId`、`(project_id,idempotency_key)` 去重为同一 Job；Renderer 不接收 Key/Auth/原始 Provider 错误。（Design §9；AGENTS.md §13.2、§14）✓ `createJobProviderIpcService` 边界 Host 完成：固定 11 频道路由、`RequestCoordinator` 同进程 requestId singleflight（同 signature 共享结果 / 异 signature → `REQUEST_ID_REUSED`）、防御性 `validateOutput`（结果偏离 DTO → `JOB_PERSISTENCE_FAILED`，零泄漏）、`ProviderProfileView→DTO` 投影（注入 `versionId`）、`mapProviderError` 把 ProviderService 标记错误归一化为稳定 code 且绝不回显原始 message/Key/Auth。`JobService` 提供 `create`（`findByIdempotencyKey` 去重 → 命中回放）、`get`/`list`（`listByStatuses` ALL + projectId 过滤）、`cancel`/`retry`（`expectedVersionId=job.id` 乐观检查 + runner/submission seam）；创建/重试委托 `JobSubmissionPort` seam 留待 #6/#7。`projectErrorCodeSchema` 扩展 9 个 Job/Provider code（加性、契约测试同步），渲染层 `ERROR_COPY` 补齐。验证：root `tsc -b` 0；Host 单测 11/11（路由/view→DTO versionId/虚拟默认/testCredential 失败/标记错误归一化/Key+Auth+SQL 零泄漏/畸形输出归一化/singleflight 共享/REQUEST_ID_REUSED/订阅/未知频道）；ESLint 0。Preload 冻结白名单已在 7.1 落定。
- [x] 7.3 先扩展启动门测试：JobRunner 领取与恢复扫描只在 `READY` 后激活；非 `READY` 时 `job`/`provider` 写命令返回 `STARTUP_WRITE_BLOCKED` 且不构造写路径；恢复扫描不在自检完成前调用 Provider。（R: desktop-workspace-foundation 新增 Scenario；Design §8）
- [x] 7.4 把 ProviderService/CredentialAdapter、类型化 Job Host 与 READY 启动门在 Main Composition Root 组装；具体阶段 JobRunner、提交器、调度和可提交恢复由 `staged-script-generation` 注入，注入前 Job 写命令稳定返回不可用且不创建空壳 Job。（Design §1、§3、§8）✓ Main 已在 READY 后幂等构造 Provider/Credential/Job Host，非 READY 返回 `STARTUP_WRITE_BLOCKED`；当前 `UNAVAILABLE_RUNNER`/`UNAVAILABLE_SUBMISSION` 明确实现安全降级，真实阶段提交与恢复重校验登记为后续 Change 的必做项。

## 8. 完整验证与 OpenSpec 收口

- [x] 8.1 运行 `pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm test:contract`、`pnpm test:integration` 与适用 `pnpm test:e2e`，记录实际通过/失败数量、网络守卫与 Mock 矩阵证据。（AGENTS.md §15、§18）✓ 全量门禁：**prettier 0**、**eslint 0**、**root `tsc -b` 0**、**UNIT 41 文件/382 测**、**CONTRACT 10 文件/69 测**、**INTEGRATION 27 文件/142 测**，合计 **593 项 Vitest 断言 GREEN**；另有 Playwright Electron E2E **4/4 GREEN**。网络守卫：3.1 锁定 `MockTextModelAdapter` 零 fetch，5.2 同一适配器继承，6.1/6.2 `QwenTextModelAdapter` 注入 fetch 断言零真实调用。Mock 矩阵证据见 5.2（8 例全矩阵 × 四条横切不变量）。2026-08-13 复核发现此前 Electron 启动失败并非 GUI/`protocol` API 不可用，而是测试进程继承宿主 `ELECTRON_RUN_AS_NODE` 后把 Electron 当普通 Node 启动；E2E 启动器现显式剔除该变量，`pnpm test:e2e` 已实际通过 **4/4**（构建 Main/Preload/Renderer 后完成项目、故障恢复与 Schema 缺失恢复流程）。
- [x] 8.2 从干净 `.vite/out` 运行 Windows x64 packaged smoke：确认打包产物可启动、凭据零明文、`job`/`provider` IPC 启动门、Mock 矩阵可重复且零真实用户目录访问。（Design §6、§7；Migration Plan）真实 `testCredential` 只允许用户自备凭据主动执行，作为发布前人工检查记录，不属于离线归档门禁。✓ 2026-08-13 使用 SHA-256=`18528bedc6a9b04bdc5efb7b803cbc3cb0e5ea6415d54046e23d464d89a00da9` 的官方 Electron 43.3.0 ZIP 离线完成 Windows x64 干净打包；扩展 packaged smoke 启动生成的 `jingxu-studio.exe`，验证 migration 1/2、四份 Schema hash/$ref、零 native addon、项目 create/update/delete/restart/restore、冻结的五命名空间白名单、Provider DTO 零 Key/Auth、未注入阶段提交器时 `job.create→JOB_SUBMISSION_UNAVAILABLE` 且 `script_stage_jobs=0`、无凭据时不创建 secrets、真实 `LOCALAPPDATA` 零访问。Mock 全矩阵由 5.2 的 8/8 可重复集成锁定；真实凭据连通性留作发布前人工检查。
- [x] 8.3 运行 `openspec validate jobrunner-qwen-text-adapter --strict` 并映射全部 Requirement/Scenario 到代码、Fixture 与测试；阻断问题清零后才可 Sync/Archive。（OpenSpec archive guidance）✓ CLI strict validate 已通过，12 项 Requirement 已映射到代码、Fixture 与测试。经审查确认，本 Change 交付通用 JobRunner、Provider/Credential、类型化 IPC Host 与未注入阶段服务时的安全降级；阶段专属 submission/commit/recovery 由后续 `staged-script-generation` Change 注入，不在本 Change 内伪造可用状态。8.1、8.2 与 8.4 证据均已完成，无归档阻断项。
  - **R1 TextModelAdapter 契约屏蔽 Provider 专有结构** → `packages/application/src/ports/text-model/{text-model-port,text-model-types,index}.ts` + `packages/application/src/ports/credential/*`；架构边界 `eslint.config.mjs`（application/domain/model-adapters 三 zone）。测试：1.2 双向非法导入探针、1.3 `text-model-ports.test.ts`/`credential-ports.test.ts`、6.1 `qwen-text-model-adapter.test.ts`（断言无 Auth header/原始错误体）。
  - **R2 Job 领取原子且仅领取方调用 Provider** → `packages/application/src/jobs/job-runner/job-runner.ts`（`claimQueued` 条件更新）+ persistence `script_stage_jobs` Repository。测试：2.1 领取原子性集成、4.1「唯一领取方」单测、5.2 5xx 终态不回退（再 run→NOT_CLAIMED）。
  - **R3 Provider 调用事务外、证据与版本短事务原子提交** → `job-runner.ts` `invoke()`（mark-sent tx → 网络事务外 → recordResponse+VALIDATING tx → 单一 UoW commit）。测试：4.1（Provider 调用事务外、mark-sent 先于网络）、2.2 Invocation Repository、5.2 STALE_INPUT（commit seam 拒绝→UoW 回滚、零版本、VALIDATING）。
  - **R4 重试与结构修复受状态机/次数/错误类型约束** → `job-runner.ts` retry/repair + `candidate-contract-pipeline.ts`；DB CHECK `transport_attempts`0–3/`structure_repair_attempts`0–1。测试：4.3 重试单测、3.3 两层契约、2.4 应用级不变量、5.2（429 重试 SUCCEEDED / 5xx 三次耗尽 / 结构修复成功与失败）。
  - **R5 取消与迟到响应不创建版本/触发重试** → `job-runner.ts` `cancel()` + `recordLateResponse`。测试：4.4 取消/迟到单测、5.2「取消+迟到」（先持久化 CANCELLED 再 abort、迟到只写 hash/late time、零版本、终态不回退）。
  - **R6 崩溃恢复按证据定终态且不自动重发未知请求** → `packages/application/src/jobs/recovery/{job-recovery,job-recovery-executor}.ts`；`register-job-provider-features.ts` 恢复 seam（#5 pending 表为空→空操作）。测试：5.1 崩溃恢复矩阵集成（QUEUED 重领/sent-uncomplete→INTERRUPTED_UNKNOWN_OUTCOME/hash 正确幂等重校验/cancel 保持/超 deadline→JOB_DEADLINE_EXCEEDED）。**#5 边界**：真实哈希复检/STALE 终态化属 #6 业务 commitHandler，#5 在 5.2 证明 JobRunner 回滚保证（design.md「JobCommitHandler」标注）。
  - **R7 真实 LLM Job 全局唯一且同项目严格串行** → `packages/application/src/jobs/concurrency/job-concurrency-gate.ts`。测试：4.5 并发门集成（全局≤1 RUNNING、同项目串行、Mock 复用同一门）。
  - **R8 凭据经 safeStorage 加密且不回流 Renderer** → `apps/desktop/src/main/adapters/credential/credential-adapter.ts`（密文独立落 `managedRoot/secrets`）+ ProviderService + IPC view→DTO 投影。测试：6.3（safeStorage 不可用阻断/DB 文件字节审计/日志白名单守卫/secrets 与 diagnostics 并列 sibling）、6.4 provider_profiles 集成、7.2 DTO 仅 last4 + Key/Auth/SQL 零泄漏。
  - **R9 Qwen Adapter 锁定固定模型 + JSON Mode + 归一化错误** → `packages/model-adapters/src/qwen/qwen-text-model-adapter.ts`。测试：6.1（`qwen3.7-plus-2026-05-26` 固定/拒漂移别名/`response_format=json_object`/64K 超限阻断/120s·300s 超时映射/401·429·5xx·超时·非法 JSON 归一化且无 Auth/原始体）。
  - **R10 Job/Provider IPC 服从启动写门与幂等** → `apps/desktop/src/main/ipc/{job-provider-service,job-provider-gate}.ts`（11 频道路由 + RequestCoordinator singleflight + `expectedVersionId`/`(project_id,idempotency_key)` 去重）+ Preload 冻结白名单。测试：7.1 逐方法契约、7.2 host 单测（singleflight 共享/REQUEST_ID_REUSED/虚拟默认/testCredential 失败/标记错误归一化）、7.4 组合（READY 后 getProfile）。
  - **R11 Job 基线以 Mock 全矩阵为 AC-V1-04 证据** → `packages/model-adapters/src/mock/mock-text-model-adapter.ts`。测试：3.1 Mock 单元（零 fetch 网络守卫）、5.2 AC-V1-04 全矩阵集成（8 例 × 四横切不变量）；真实调用仅限 `provider.testCredential` 低成本连通性（7.2 host 单测）。
  - **R12(desktop-workspace-foundation) JobRunner 调度与 IPC 受启动门约束** → `register-job-provider-features.ts`（非 READY→`STARTUP_WRITE_BLOCKED` facade、`ensureRegistered()` 在 `writeEnabled` 后幂等激活）+ `main.ts` runtimeIpc/启动尾衔接。测试：7.3 启动门、7.4 组合（非 READY 阻断、UoW 未就绪不构造、幂等激活）。零阻断；Sync/Archive 前置条件（8.1 门禁全绿、8.4 字节审计）见对应条目。
- [x] 8.4 最终 diff 证明 `0001_initial.sql`、`0002_project_command_receipts.sql` 与四份根 Schema 字节未变，未引入五编剧阶段、ScriptService、SHOT_CONTRACT 生成、EpisodeValidator、ProducibilityService 或 V2/V3 能力。（Proposal Non-Goals）✓ **字节未变**：`git diff <merge-base>..HEAD --stat` 对 2 份 migration（`packages/persistence/resources/migrations/0001_initial.sql`、`0002_project_command_receipts.sql`）+ 4 份根 Schema（repo 根 `镜序Studio_V1_{EpisodeStoryboardExport,ProjectTransferBundle,ScriptStageOutput,ShotContract}.schema.json`）输出 **EMPTY = 零字节变更**；`git status --porcelain` 同样 EMPTY（无工作树改动）。3 份内部 `packages/validation/resources/schemas/v1/*.schema.json` 是 **Change #4**（commit `cde8538 feat(validation): lock V1 schema resources`）新增、非 #5 触碰；`git log <mb>..HEAD -- <migration+schema paths>` 仅返回 `cde8538` 一条（#5 的 jobrunner/persistence/model-adapters/IPC 提交零触及）。`ShotContract.schema.json` 在 **main 已存在**（`git cat-file -e main:<path>` 通过），为既有 PRD 根 Schema。**零 V2/V3 实现泄漏**：对新增 `.ts` 源码行扫描 `ScriptService|EpisodeValidator|ProducibilityService|stage_heads|stageHeads|ModelShotBatchCandidate|ShotContractGenerator|generateShotContract` → **0 命中**（未实现五编剧链/分镜生成/可生产性/校验器）。`SHOT_CONTRACT` 的 5 处出现经逐条核验均为**既有 DB CHECK 的类型级镜像**，非新能力：`script_stage_jobs.stage` 在 `0001_initial.sql:93` 已含 `'SHOT_CONTRACT'`（与 `prompt_templates.stage` 第 234/248 行一致），故 `packages/contracts/src/stage.ts` 的 `scriptStageSchema` z.enum、`packages/contracts/src/job-provider-api.ts` 的 stage 枚举面、`packages/persistence/src/job/row-mapper.ts` 的合法 stage 数组必须包含它以忠实映射 DB；`stage.ts` 注释明确「SHOT_CONTRACT 属于分镜阶段，由后续 Change 接入，本 Change 的 JobRunner 与 TextModelAdapter 不对其赋予额外业务语义」。`shot_contracts/0/status` 仅出现在 `packages/test-fixtures/src/v1-schema-fixture-cases.ts`（V1 导出 Schema 校验 fixture，EpisodeStoryboardExport 合法含 shot_contracts），非生成代码。Non-Goals 全部守住。
