# 镜序 Studio 原型对齐视觉 QA

## 对照来源

- 原型剧本工作区：`designs/jingxu-studio-prototype/prototype-script-legible.png`
- 正式剧本工作区：`openspec/changes/prototype-aligned-workspace-ui/implementation-script.png`
- 原型模型服务：`designs/jingxu-studio-prototype/prototype-settings.png`
- 正式模型服务：`openspec/changes/prototype-aligned-workspace-ui/implementation-settings.png`

## 视口与状态

- 原型图片：1728 × 1065。
- 正式 Electron 截图：1728 × 1064，设备像素比 1。
- 密度归一化：对照时忽略原型最末 1px，只比较共同的 1728 × 1064 区域。
- 共同状态：Windows 桌面、深色主题、项目“大富翁的每一天”、全局导航展开。
- 可解释状态差异：原型剧本为“故事概念已确认”，正式截图使用隔离 E2E 新项目，故事概念未开始；原型 Provider 为已配置，正式截图不注入用户凭据而显示未配置。状态差异不参与布局、字号和组件几何判定。

## 对照范围

### 全屏

- 216px 全局侧栏、64px 顶部定位栏。
- 项目内 264px 流程栏、自适应创作区、368px 上下文检查器。
- 设置页内容从侧栏后 32px 开始，模型服务为左对齐纵向横卡。

### 聚焦区域

- 上下文检查器标题、常用/版本与锁/任务标签及正文可读性。
- 六阶段流程与剧本五子阶段层级。
- 模型卡图标、Provider、76 × 36 配置状态、当前模型、脱敏凭据与管理入口。
- 页面中不存在重复的“项目进度自动保存 · 最近保存于刚刚”。

## 必要一致性结论

- 信息架构：一致。正式 Renderer 使用原型的全局导航、六阶段流程和三栏工作台。
- 间距与尺寸：一致。E2E 精确断言 216/264/368px，设置内容左对齐；窄窗口在 1360、980、720px 断点重排。
- 字号：一致。全局导航为 15px，上下文正文为 13px，辅助文字不低于 12px。
- 状态槽：一致。模型配置状态固定为 76 × 36，不因“已配置/未配置”溢出。
- 安全展示：一致。完整 API Key、本地路径、FFmpeg 命令和 Provider 原始响应不进入用户可见区域。

## 对照历史

1. 第一轮：发现分镜内容与剧本同页纵向堆叠、设置表单默认全部展开，判定为 P1；改为场景剧本确认后再显示分镜，并将模型卡改为默认折叠摘要。
2. 第二轮：发现设置页多余大标题顶栏、模型卡缺少当前模型/凭据列、检查器缺少三标签，判定为 P2；补齐原型结构并保留真实业务状态。
3. 第三轮：发现配置状态只有文字、未形成固定槽，判定为 P2；增加 76 × 36 状态徽标并重新完成同视口 Electron 截图对照。
4. 最终复核：未发现剩余 P0、P1 或 P2 视觉问题；内容差异仅来自明确记录的 E2E 业务状态差异。

## 最终结果

passed
