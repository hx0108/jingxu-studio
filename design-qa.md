# 镜序 Studio 原型对齐视觉 QA

## 对照来源

- 视觉来源（2026-09-10 起）：CINE 剪辑台方向 `designs/jingxu-studio-redesign/`（screens/ 下 11+ 张评审截图）。
- 几何与状态断言：`apps/desktop/e2e/prototype-aligned-workspace.e2e.spec.ts`（E2E 输出 implementation-script.png / implementation-settings.png）。
- 历史轮次对照（旧原型，已被 CINE 取代）：`designs/jingxu-studio-prototype/` 与 `openspec/changes/prototype-aligned-workspace-ui/`。

## 视口与状态

- 正式 Electron 截图：1728 × 1064，设备像素比 1。
- 共同状态：Windows 桌面、深色主题、项目“大富翁的每一天”、全局导航展开。
- 可解释状态差异：原型为演示数据，正式截图使用隔离 E2E 新项目（故事概念未开始、Provider 未配置）。状态差异不参与布局、字号和组件几何判定。

## 对照范围

### 全屏

- 208px 全局侧栏、48px 顶部定位栏。
- 项目内 264px 流程栏、自适应创作区、360px 上下文检查器。
- 设置页内容从侧栏后 32px 开始，模型服务为左对齐纵向横卡。

### 聚焦区域

- 上下文检查器标题、常用/版本与锁/任务下划线页签及正文可读性。
- 六阶段流程：等宽序号、激活项 2px tally 条与 6px 方形指示标。
- 模型卡：线框图标、Provider、文字化配置状态（6px 方标 + 文案，无徽标盒）、当前模型、脱敏凭据与管理入口。
- 全局导航 16px 线性图标 + 激活 tally；无字符图标框。
- 页面中不存在重复的“项目进度自动保存 · 最近保存于刚刚”。

## 必要一致性结论

- 信息架构：一致。正式 Renderer 使用 CINE 原型的全局导航、六阶段流程和三栏工作台。
- 间距与尺寸：一致。E2E 精确断言 208/264/360px 与 48px 顶栏，设置内容左对齐；窄窗口在 1360、980、720px 断点重排。
- 字号：一致。全局导航为 13px，上下文正文为 13px，辅助文字不低于 12px。
- 状态呈现：一致。状态一律为“6px 方标 + 文字”，无药丸徽标（E2E 断言 border-radius 0 与 ::before 6px 方标）。
- 数字排印：一致。阶段序号与时间线数值用等宽字体。
- 安全展示：一致。完整 API Key、本地路径、FFmpeg 命令和 Provider 原始响应不进入用户可见区域。

## 对照历史

1. 第一轮：发现分镜内容与剧本同页纵向堆叠、设置表单默认全部展开，判定为 P1；改为场景剧本确认后再显示分镜，并将模型卡改为默认折叠摘要。
2. 第二轮：发现设置页多余大标题顶栏、模型卡缺少当前模型/凭据列、检查器缺少三标签，判定为 P2；补齐原型结构并保留真实业务状态。
3. 第三轮：发现配置状态只有文字、未形成固定槽，判定为 P2；增加 76 × 36 状态徽标并重新完成同视口 Electron 截图对照。
4. 最终复核：未发现剩余 P0、P1 或 P2 视觉问题；内容差异仅来自明确记录的 E2E 业务状态差异。
5. 第五轮（2026-09-10，CINE 全量对齐）：首落地只移植了 tokens 层，部署观感与评审原型有较大差距（药丸徽标、大写眉标、64px 顶栏、216/368 栏宽、字符图标框均未动）。本轮把组件级设计语言全量移植：状态文字化、眉标去间距、检查器下划线页签、48px 顶栏、208/264/360 三栏、16px 线性导航图标、等宽阶段序号与时间线数值、平涂占位帧；E2E 断言与本文档同步改写。

## 最终结果

passed
